# ctx CI/CD Pipeline
# github.com/GottZ/ctx — The memory your LLM pretends to have.
# GottZ 4-Way RRF | GottZ Scope Model | GottZ Guard
#
# Branch-Strategie: root (Haupt-Branch), keine main/master-Konvention.
# Alle Stages laufen auf: push to root, PR to root, workflow_dispatch.

name: ctx CI/CD

on:
  push:
    branches: [root]
  pull_request:
    branches: [root]
  workflow_dispatch:
  # Nightly cadence for the PV10 live tier (design 06 §4.7/§9.5, decision E3).
  # The whole workflow re-runs at 03:00 UTC, but only web-live is scheduled-
  # gated to actually do live work; docker is gated OFF schedule so a nightly
  # run stays a verification, not a redundant image re-push.
  schedule:
    - cron: "0 3 * * *"

# Deny-all default — jeder Job deklariert eigene Permissions.
permissions: {}

# Schedule runs get their OWN concurrency group (wave C-W3): the nightly
# tiers (web-live release gate, web-lhci timing trend) must survive a root
# push around 03:00 UTC — in the shared group the push would cancel the
# schedule run and the tag rule (green nightly before a version tag) would
# block the morning release until a manual re-dispatch. Two parallel runs on
# the same ref cannot conflict: docker (image push) is gated OFF schedule.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}${{ github.event_name == 'schedule' && '-nightly' || '' }}
  cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' }}

env:
  GO_VERSION: "1.27"
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/gottz/ctx

jobs:
  # ─── Stage 1: Lint ────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20 # v9.2.0
        with:
          # Gepinnt statt latest (2026-08-05): ein frisches staticcheck-Release
          # stellte SA5011 auf das korrekte `if x == nil { t.Fatal }`-Muster
          # scharf und rötete drei unveränderte Test-Dateien — Lint-Wahrheit
          # muss lokal (pre-commit, Agenten-Gates) und CI dieselbe sein.
          # Bump = bewusster Commit, nie ein stiller Action-Download.
          #
          # Bump auf v2.13.2 (2026-09-19, Dependency-Sweep): v2.13.0 bringt
          # staticcheck 0.8.0, das zwei echte SA1019-Funde in
          # go/internal/oidc/verify.go hatte (deprecated ecdsa.PublicKey.X/.Y
          # auf dem JWK-Pfad) — im selben Zug behoben, sonst wäre der Bump ein
          # roter Job statt eines Pins. v2.13 ist zugleich die Voraussetzung
          # für Go 1.27: golangci-lint lintet nur Go-Versionen bis zu der, mit
          # der es selbst gebaut wurde, und v2.11.4 bricht gegen ein Modul mit
          # `go 1.27.0` mit "the Go language version used to build
          # golangci-lint is lower than the targeted Go version" ab.
          version: v2.13.2
          working-directory: go
          args: --timeout=5m
          # Kein Analyse-Cache (2026-08-26): der Cache-Key der Action trägt das
          # Go-Patchlevel nicht. Als der Runner von go1.26.6 auf go1.26.7 wechselte,
          # wurde der unter 1.26.6 gefüllte Cache (Key …-go-2955-2d35…) restauriert
          # und staticcheck meldete SA5011 auf zehn unveränderte, korrekte
          # `if x == nil { t.Fatal }`-Stellen (v5.7.0-Lauf 32944991742; Nachtlauf
          # 03:22Z mit 1.26.6 und demselben Key war grün). Dieselbe Klasse wie die
          # lokale Cache-Kontamination bei parallelen Worktrees — und vermutlich
          # auch der Vorfall vom 05.08. (s. o.). Lint dauert ohne Cache ~1 min.
          skip-cache: true
          only-new-issues: ${{ github.event_name == 'pull_request' }}
          # NZ-1: KEIN zweiter Lauf mit --build-tags=integration. Der Tag steht
          # in .golangci.yml (`run.build-tags`), also fährt dieser Schritt ihn
          # schon — genauso wie .hooks/pre-commit und jedes lokale
          # `golangci-lint run`. Ein zweiter Aufruf hier wäre dieselbe Wahrheit
          # zum doppelten Preis und die zweite Stelle, an der der Tag fehlen
          # kann. Vor NZ-1 lief der Job tag-los und sah keine einzige
          # `//go:build integration`-Datei (111 Funde, 3 davon Layer-Verstöße).

      # T02-7: der Tripwire des Tree-Shaking-Runs. deadcode.sh vergleicht die
      # Erreichbarkeitsanalyse gegen go/deadcode-allow.txt und blockiert in
      # BEIDE Richtungen (neuer toter Fund / gelistete Zeile lebt wieder).
      # Version gepinnt aus demselben Grund wie golangci-lint oben: ein
      # @latest-Bump würde die Allowlist ohne Commit röten. KEIN
      # working-directory — das Skript wechselt selbst nach go/, sonst tragen
      # die Ausgabepfade ein cwd-Präfix und kein Allowlist-Schlüssel matcht.
      - name: deadcode allowlist gate
        run: |
          go install golang.org/x/tools/cmd/deadcode@v0.50.0
          bash "$GITHUB_WORKSPACE/deadcode.sh"

      # T02-13: die zweite Ratsche. Derselbe Lauf OHNE `-test` beantwortet die
      # andere Frage — welche Produktions-Symbole leben nur, weil ein Test sie
      # ruft (Klasse T). Eigene Policy-Datei go/deadcode-testonly-allow.txt,
      # jede Zeile mit Grund, blockierend in beide Richtungen wie oben. Der
      # Schritt steht bewusst HINTER dem ersten: der testonly-Lauf subtrahiert
      # go/deadcode-allow.txt, und diese Subtraktion ist nur exakt, solange das
      # erste Gate grün ist. Kein eigenes `go install` — das Binary von oben ist
      # dasselbe, und ein zweiter Pin könnte auseinanderlaufen.
      - name: deadcode test-only ratchet
        run: |
          bash "$GITHUB_WORKSPACE/deadcode.sh" testonly

      # Beobachtung, kein Gate: dupl misst Klon-Masse und schreibt sie in die
      # Job-Summary. `if: always()` liefert die Zahl auch dann, wenn ein Gate
      # davor rot war — gerade dann ist sie interessant. `|| true`, weil dupl
      # bei Funden rc≠0 liefert und der Report niemandem den Build brechen soll.
      - name: dupl report (nicht blockierend)
        if: always()
        working-directory: go
        run: |
          go install github.com/mibk/dupl@v1.1.0
          dupl -t 80 $(find internal cmd -name '*.go' -not -name '*_test.go') \
            | tee -a "$GITHUB_STEP_SUMMARY" || true

  # ─── Stage 1b: Web (frontend gate, parallel to Lint) ─────────────────────────
  # Wave PV8 (design 06 §7): PRs check the frontend for the first time.
  # Every bun step runs inside the digest-pinned toolchain container built
  # from e2e/toolchain.lock — the SINGLE digest source (decision E4), parsed
  # by go/web/e2e-container.sh (the same implementation e2e-visual.sh uses;
  # no second copy of the lock mechanics in this YAML). That container is the
  # canonical render environment, so @visual pixel asserts run here
  # (CTX_E2E_CONTAINER=1 is set by the runner script).
  #
  # Command parity with the release build (go/Dockerfile stage 0):
  # `bun install --frozen-lockfile` and `bun run check` are byte-identical;
  # `bun run build` runs inside the e2e step via the Playwright webServer
  # (with VITE_E2E=1 — the plain production build stays covered by the
  # Docker stage). Known, deliberate divergence: the release stage uses
  # oven/bun alpine (musl), the toolchain image the glibc bun binary on the
  # Playwright noble base (design 06 §4.4/S4).
  #
  # ── Sharding preparation (design 06 §6.3, wave PV11 — PREPARED, NOT ACTIVE) ──
  # The reporter foundation (blob) is already laid (playwright.config.ts), so
  # activating sharding is a YAML DIFF, not a rebuild. It stays dormant because
  # the calibrated budget (.github/e2e-budget.json: e2e ~77s, budget 160s) sits
  # far below the 270s e2e part budget — sharding activates only after the §6.3
  # trigger (three consecutive runs over the e2e part budget) OR by flipping the
  # repo variable `vars.E2E_SHARD == '1'`. The activation diff, verbatim:
  #
  #   web:
  #     strategy:
  #       fail-fast: false
  #       matrix: { shard: [1, 2, 3, 4] }
  #     steps:
  #       ...
  #       - name: e2e (Playwright in pinned container)
  #         run: bash go/web/e2e-container.sh bun run test:e2e -- \
  #                --shard=${{ matrix.shard }}/4          # blob reporter is per-shard
  #       # upload each shard's e2e/.results/blob as blob-${{ matrix.shard }}
  #   web-merge:                                          # new fan-in job
  #     needs: [web]
  #     steps:
  #       - download all blob-* artifacts
  #       - bun x playwright merge-reports --reporter html ./blobs
  #
  # Activation-day gate (design 06 §7-PV11 (c)): a 4×blob → merge-reports probe
  # must yield the SAME total test count as a single-run report. Not run here —
  # sharding is dormant, and a real 4-shard probe means four full e2e runs
  # (deliberately deferred until the trigger fires; not a rebuild).
  web:
    name: Web
    runs-on: ubuntu-latest
    # Hard deck above the 10-min budget fail (design 06 §6.3).
    timeout-minutes: 12
    permissions:
      contents: read
    steps:
      - name: Record job start (budget measurement, design 06 §6.3)
        run: echo "CTX_JOB_STARTED_AT=$(date +%s)" >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # Full history: the baseline marker gate diffs the whole PR/push
          # range and reads per-commit messages (design 06 §5.5 layer 2).
          fetch-depth: 0

      # Enforcement that survives --no-verify and dead local hooks: baseline
      # changes (__screenshots__, a11y debt growth) require the [baseline]
      # marker on every touching commit, or the PR label `baseline-update`.
      # Runs where a range exists: PRs (base...head) and pushes with a real
      # `before` SHA. Skipped for branch-creation pushes (before = zero SHA)
      # and workflow_dispatch — there is no range to check there.
      - name: Baseline marker gate (design 06 §5.5 layer 2)
        if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.event.before != '0000000000000000000000000000000000000000')
        env:
          RANGE_BASE: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.event.pull_request.base.ref) || github.event.before }}
          PR_LABELS: ${{ github.event_name == 'pull_request' && join(github.event.pull_request.labels.*.name, ' ') || '' }}
        run: bash .github/scripts/baseline-marker-gate.sh

      - name: Build pinned toolchain container (e2e/toolchain.lock)
        run: bash go/web/e2e-container.sh bun --version

      # Byte-identical to go/Dockerfile stage 0 — the v4.1.0 lesson: bun.lock
      # drift was CI-invisible until the Docker build; this catches it on PR.
      - name: Install (bun, frozen lockfile)
        run: bash go/web/e2e-container.sh bun install --frozen-lockfile

      # K9 steps (design 05 → this job): 05 contributes lint:css and the
      # contrast matrix as named job components. Reading of 05 (§1 DoD +
      # masterplan K9): no separate MECHANISM is demanded — the contrast
      # matrix is a vitest test (tokens/contrast-matrix.test.ts) and would be
      # covered by `bun run test` below; `bun run check` re-runs lint:css.
      # Both get named steps anyway for failure attribution (the K9 contract
      # stays visible in the job UI); each re-run costs single-digit seconds.
      - name: Lint CSS (token gate + inline gate — K9)
        run: bash go/web/e2e-container.sh bun run lint:css

      - name: Contrast matrix (WCAG token gate — K9)
        run: bash go/web/e2e-container.sh bun run test tokens/contrast-matrix.test.ts

      - name: Check (lint:css + svelte-check + tsc)
        run: bash go/web/e2e-container.sh bun run check

      - name: Unit tests (vitest)
        run: bash go/web/e2e-container.sh bun run test

      # Full suite incl. @visual: this is the canonical pinned container, the
      # committed baselines are compared here (design 06 §5.5: CI has NO
      # update path — --update-snapshots exists only in e2e-visual.sh).
      - name: e2e (Playwright in pinned container)
        # Nightly (schedule) + on-demand (dispatch) INCLUDE the @quarantine
        # specs — quarantine means observed, not forgotten (design 06 §5.4,
        # wave PV11). PRs/pushes leave CTX_E2E_QUARANTINE empty, so the config
        # grepInvert excludes them from the per-PR gate. e2e-container.sh passes
        # the var through into the pinned container.
        env:
          CTX_E2E_QUARANTINE: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && '1' || '' }}
        run: bash go/web/e2e-container.sh bun run test:e2e

      # ── Bundle byte budget gate (PLc, wave C-W2) ──────────────────────────
      # Byte truth = the RELEASE encoding: on-disk .br siblings from a build
      # WITHOUT VITE_E2E (the bytes go/web/web.go actually serves). At this
      # point dist/ holds the VITE_E2E build from the Playwright webServer
      # (test hooks included — wrong bytes for a budget verdict), so the gate
      # step pair rebuilds fresh and measures immediately after; nothing runs
      # in between. Placement AFTER e2e (masterplan C-W2): the webServer build
      # can never overwrite the measured artifacts. `if: !cancelled()` keeps
      # byte-bloat attribution visible even when e2e is red; the gate itself
      # is guarded on the release build's outcome so it can never measure a
      # stale VITE_E2E dist/. PLc is the PR byte authority — the Lighthouse
      # layer (C-W3) is nightly, warn-only, and never judges bytes.
      - name: Release build (byte truth — no VITE_E2E)
        id: release_build
        if: ${{ !cancelled() }}
        run: bash go/web/e2e-container.sh bun run build

      - name: Bundle byte budget gate (PLc)
        if: ${{ !cancelled() && steps.release_build.outcome == 'success' }}
        run: bash go/web/e2e-container.sh bun run test:budget

      - name: Flake annotation (report.json retry statistics)
        if: ${{ !cancelled() }}
        run: bash .github/scripts/flake-annotations.sh

      - name: Budget check (design 06 §6.3)
        if: ${{ !cancelled() }}
        run: bash .github/scripts/e2e-budget.sh

      # HTML report from the blob reporter output — git hosting renders no
      # PNG diffs inline; without this artifact baseline review is blind
      # (design 06 §4.8). Runs in the same container; `bun x` (not `bunx` —
      # the toolchain image ships only the bun binary, no bunx symlink).
      - name: Render HTML report from blob
        if: ${{ !cancelled() }}
        run: |
          if [ -d go/web/e2e/.results/blob ]; then
            bash go/web/e2e-container.sh bun x playwright merge-reports --reporter html ./e2e/.results/blob
          else
            echo "no blob output (e2e step did not run) — skipping HTML render"
          fi

      - name: Upload e2e report + diffs
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: web-e2e-report
          path: |
            go/web/playwright-report/
            go/web/e2e/.results/report.json
          # Mock-tier retention 14 days (design 06 §4.8); the live tier
          # (wave PV10) gets 3 — real keys flow only there.
          retention-days: 14
          if-no-files-found: ignore

  # ─── Stage 1c: Web Live Tier (PV10, nightly + on-demand) ─────────────────────
  # Wave PV10 (design 06 §4.7/§9.5): the ≤15 @live specs run against a REAL,
  # throwaway ctxd + Postgres (docker-compose.e2e.yml), seeded ONLY through
  # production write paths (fail-closed target gate, §3.6). It proves classes
  # the mock `web` job cannot — server enforcement (tenant isolation), fixture-
  # shape truth (W10), real SSE transport — so it is intentionally OFF the
  # per-PR path: nightly (schedule) + on-demand (dispatch) + PR opt-in via the
  # `e2e-live` label (decision E3). run-live.sh needs only docker on the host;
  # bun + Playwright run INSIDE the pinned toolchain image (--network host).
  #
  # RELEASE-GATE RULE (design 06 §9.5, README): a version tag is pushed only
  # after a GREEN nightly run of THIS job — the live-tier extension of the
  # repo's "CI is truth, not local" rule.
  web-live:
    name: Web Live Tier
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'e2e-live'))
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Live tier (throwaway compose stack + seed + @live specs)
        run: bash go/web/e2e/live/run-live.sh

      # Live traces retention 3 (design 06 §4.8): they record Bearer headers +
      # the reveal-once owner_key, but every key is valid ONLY against the
      # job-local instance that just died — impact null (§3.6 invariant 3).
      - name: Upload live-tier report + traces
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: web-live-report
          path: go/web/e2e/live/.results/
          retention-days: 3
          if-no-files-found: ignore

  # ─── Stage 1d: Web Trend + History Budget (PV11, nightly) ────────────────────
  # Wave PV11 (design 06 §6.3): two nightly bookkeeping measurements that dock
  # onto the nightly run without touching the release-gating web-live job.
  #   - e2e DURATION + flaky trend line, from the mock-tier `web` job's
  #     report.json (one JSON line per run, retention 90d — §4.7/§6.3);
  #   - screenshot HISTORY blob-volume budget (git rev-list over the full
  #     history, needs fetch-depth: 0 — §3.1/§6.3).
  # A DEDICATED job (not a step in web-live): web-live runs a 30-min live stack
  # and owns the LIVE report.json (e2e/live/.results), while the trend wants the
  # MOCK-tier duration from the `web` job — folding it into web-live would couple
  # trend bookkeeping to the release gate and force a mock-artifact download into
  # the live job. A ~2-min job stays independently green/red. Nightly + dispatch
  # only (no per-PR trend noise). `needs: [web]` for the report.json artifact —
  # a red `web` skips the trend (no point trending a failed run).
  web-trend:
    name: Web Trend + History Budget
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [web]
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    steps:
      - name: Checkout (full history for the screenshot blob-volume measurement)
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Download mock-tier e2e report (from the `web` job)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: web-e2e-report
          path: web-report

      - name: e2e duration + flaky trend line (design 06 §6.3)
        run: |
          REPORT=$(find web-report -name report.json -print -quit 2>/dev/null || true)
          REPORT="${REPORT:-go/web/e2e/.results/report.json}" TREND_DIR=trend \
            bash .github/scripts/e2e-trend.sh

      - name: Screenshot history budget (design 06 §3.1/§6.3)
        # Nightly runs on root ⇒ HEAD==root; the script defaults to the `root`
        # ref when present, HEAD otherwise. Annotates >=60 MB, escalation-due
        # >=150 MB — never an auto-fail (§6.3: the orphan-branch/LFS decision is
        # the User's, taken WITH the measurement).
        run: bash .github/scripts/history-budget.sh

      - name: Upload trend line
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: web-trend
          path: trend/
          # Generous retention (design 06 §6.3): one line per run reconstructs
          # the duration/flaky trend from the retained set.
          retention-days: 90
          if-no-files-found: ignore

  # ─── Stage 1e: Web LHCI Nightly (timing trend, wave C-W3) ────────────────────
  # LHCI is the lab TIMING probe — nightly-only, warn-only, NEVER a PR gate
  # and NEVER a byte authority (DECISIONS C-E1/C-E2, keeps 06-E6: bytes are
  # judged by the PLc budget gate in the `web` job). A DEDICATED job, not a
  # step in `web` and without `needs:` — it must stay green/red independently
  # (web-trend dies with a red `web`; this must not). Runs the same pinned
  # toolchain container; Chromium + node come from the image (no
  # chrome-launcher dep, CHROME_PATH globbed in lighthouserc.cjs). Lab noise
  # floor is ±20-40% on shared runners — the lhr artifacts feed the 90-day
  # trend (C-W4), single runs are never interpreted. timeout-minutes is the
  # wall-clock budget: a hung Chromium turns the job visibly red, never a
  # silent multi-hour runner burn (C2-3).
  web-lhci:
    name: Web LHCI Nightly (timing trend)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # workflow_dispatch is included deliberately: it is the only way to test
    # the nightly path without waiting for 03:00 UTC (same rationale as
    # web-live/web-trend).
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Build pinned toolchain container (e2e/toolchain.lock)
        run: bash go/web/e2e-container.sh bun --version

      - name: Install (bun, frozen lockfile)
        run: bash go/web/e2e-container.sh bun install --frozen-lockfile

      - name: Release build (no VITE_E2E — LHCI serves dist/ statically)
        run: bash go/web/e2e-container.sh bun run build

      - name: LHCI (3 runs, median, warn-only)
        run: bash go/web/e2e-container.sh bun run perf:lhci

      # e2e-container.sh writes .lhci/ as root (the pinned container runs as
      # root, repo bind-mounted at /work). The host-side trend step below runs
      # as the runner user and cannot mkdir into a root-owned dir — reclaim
      # ownership first. Non-fatal: a missing .lhci/ means perf:lhci already
      # failed and owns the red; this step must never introduce a new one.
      - name: Reclaim LHCI output ownership (container writes .lhci/ as root)
        if: ${{ !cancelled() }}
        run: |
          if [ -d go/web/e2e/perf/.lhci ]; then
            sudo chown -R "$(id -u):$(id -g)" go/web/e2e/perf/.lhci
          fi

      # Trend line (wave C-W4): ONE JSON line from the representative (median)
      # run — the 90-day artifact set reconstructs the timing trend. Noise
      # floor ±20-40% (annotation only, never a gate — C1-M2/M3).
      - name: LHCI timing trend line (wave C-W4)
        if: ${{ !cancelled() }}
        run: bash .github/scripts/lhci-trend.sh

      - name: Upload trend line (90d)
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: web-lhci-trend
          path: go/web/e2e/perf/.lhci/trend/
          retention-days: 90
          if-no-files-found: ignore
          # .lhci is a dot-dir — upload-artifact excludes hidden paths by default.
          include-hidden-files: true

      # if-no-files-found: error — an LHCI run that produced no lhr JSON is a
      # broken measurement and must be visibly red, never silently green.
      - name: Upload lhr reports
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: web-lhci-reports
          path: go/web/e2e/perf/.lhci/
          retention-days: 14
          if-no-files-found: error
          # .lhci is a dot-dir — upload-artifact excludes hidden paths by default.
          include-hidden-files: true

  # ─── Stage 2: Unit Tests ──────────────────────────────────────────────────────
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: Run unit tests
        working-directory: go
        run: |
          # Coverage + -v only on nightly/dispatch (2026-08-20): atomic
          # coverage instruments every memory access (~10-30% overhead under
          # -race) and nobody consumes the artifact on push runs — the nightly
          # keeps the observability. Failure output stays visible without -v
          # (go test prints failing tests' logs regardless).
          EXTRA_FLAGS=""
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            EXTRA_FLAGS="-v -coverprofile=coverage.out -covermode=atomic"
          fi
          go test \
            $EXTRA_FLAGS \
            -short \
            -race \
            -count=1 \
            -timeout=5m \
            ./...

      - name: Upload coverage
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: coverage-unit
          path: go/coverage.out
          retention-days: 7

  # ─── Stage 2b: Cross-compile gate ────────────────────────────────────────────
  # Issue #40 (bug 4): the release matrix builds only cmd/ctx, whose import
  # graph skips embedmigration and goldbench, so a unix-only syscall in either
  # package broke `go build ./...` on Windows for months without a single CI
  # job noticing. This gate compiles AND vets every package (tests included)
  # for the non-Linux targets the release ships — on a Linux runner, since a
  # cross-compile needs no emulator; it is exactly what a developer on those
  # platforms hits first.
  cross-compile:
    name: Cross-compile (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - { goos: windows, goarch: amd64 }
          - { goos: darwin, goarch: arm64 }
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: Build + vet every package for ${{ matrix.goos }}/${{ matrix.goarch }}
        working-directory: go
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          go build ./...
          go vet ./...

  # Native Windows run of the two packages that carry a windows-tagged
  # implementation (embedmigration/disk_windows.go, goldbench/dumpfile_windows.go):
  # the cross-compile above proves they build, only a Windows runner proves
  # GetDiskFreeSpaceEx and LockFileEx behave like their unix counterparts
  # under the packages' own tests. autocrlf is forced off before checkout so
  # golden fixtures keep their bytes.
  windows-unit-tests:
    name: Unit Tests (windows, tagged packages)
    runs-on: windows-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Keep checkout bytes intact (no CRLF conversion)
        run: git config --global core.autocrlf false

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: Run the windows-relevant package tests
        working-directory: go
        shell: bash
        run: go test -short -count=1 -timeout=5m ./internal/embedmigration/ ./internal/goldbench/

  # ─── Stage 3: Integration Tests ───────────────────────────────────────────────
  # Sharded 5-way (2026-08-20), 6-way since 2026-08-29 (`distill`): the
  # un-sharded job serially ran ~22m and was the whole pipeline's critical path
  # (next-longest job: web at ~4.5m).
  # Public repo ⇒ parallel standard runners are free, so the shards cost
  # nothing and the wall-clock drops to the slowest shard. Cut by measured
  # package runtimes UNDER THE TEMPLATE HARNESS + -p 1 (2026-08-20, local:
  # guard 211s, overview 167s, events 55s, dream 35s, rrf 25s — the original
  # 4-way cut predated the template harness, which shrank handler/store and
  # left guard+overview carrying ~77% of the old heavy shard). guard runs
  # alone; heavy keeps overview/events/rrf/dream; distill carries
  # internal/distillsource, whose ctxcheckpoint package has needed 615s locally
  # since the TestSessionsHorizonCrossoverAtScale scale test landed (ddb6c2a9)
  # — it used to ride in `rest`, where it threatened BOTH budgets at once: its
  # own 15m per-package -timeout, and — as one more serial package under -p 1 —
  # the job cap of a shard that already ran every remaining package.
  # `rest` is the COMPLEMENT computed via go list at run time, so a NEW package
  # with integration tests always lands in a shard automatically — never
  # silently untested. The chosen package list is echoed into the log so every
  # run shows what it actually covered.
  # Budgets are PER SHARD (matrix.pkg_timeout / matrix.job_timeout) so one slow
  # shard never has to weaken the hang detection of the fast ones.
  integration-tests:
    name: Integration Tests (${{ matrix.shard }})
    runs-on: ubuntu-latest
    # 25 for the standard shards (was 20): the handler package alone may take up
    # to its 15m package budget on slow nightly runners — see the -timeout note
    # below. distill runs on 40; the reasoning sits in its matrix entry.
    timeout-minutes: ${{ matrix.job_timeout }}
    needs: [unit-tests]
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - shard: handler
            packages: "./internal/handler/..."
            pkg_timeout: 15m
            job_timeout: 25
          - shard: store
            packages: "./internal/store/..."
            pkg_timeout: 15m
            job_timeout: 25
          - shard: guard
            packages: "./internal/guard/..."
            pkg_timeout: 15m
            job_timeout: 25
          - shard: heavy
            packages: "./internal/overview/... ./internal/events/... ./internal/rrf/... ./internal/dream/..."
            pkg_timeout: 15m
            job_timeout: 25
          - shard: distill
            # ctxcheckpoint: 615s locally, isolated and green (2026-08-29). The
            # 30m package budget is 2.9x that — it survives a runner up to 2.9x
            # slower than that host, while a real hang still fails inside the
            # job. For scale: the measured CI/local ratio of the other shards in
            # run 33230621555 was 1.03-1.09 (guard 218s CI vs 211s local; heavy
            # 306s vs 282s), but that ratio comes from many short,
            # container-bound tests — not from one long seed-heavy scale test on
            # a 7GB runner — so this budget is sized for the pessimistic ~2x
            # envelope (1231s), not for 1.1x.
            # job_timeout 40 > 30m + the 37-64s of per-job overhead measured in
            # that same run (+ headroom for a cold image cache, whose key is the
            # manifest hash, and for the post-mortem step). Keeping the job cap
            # ABOVE the package budget is deliberate: the package timeout fires
            # first and dumps every goroutine, whereas a job kill leaves nothing
            # to diagnose.
            # The two sibling packages carry no integration-tagged tests and
            # cost 0.09s together (measured 2026-08-29), so this shard's budget
            # is effectively ctxcheckpoint's alone.
            packages: "./internal/distillsource/..."
            pkg_timeout: 30m
            job_timeout: 40
          - shard: rest
            # Complement of the explicit shards above — computed at run time.
            packages: ""
            pkg_timeout: 15m
            job_timeout: 25

    env:
      TESTCONTAINERS_RYUK_DISABLED: "false"
      TESTCONTAINERS_PULL_POLICY: "default"
      CONTEXT_DB: context_store
      CONTEXT_DB_USER: context_user
      CONTEXT_DB_PASSWORD: testpassword

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: Cache testcontainers Docker image
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/docker-images
          # Der Manifest-Hash im Key ist die Invalidierung: das Contract-Manifest
          # pinnt u. a. die Extension-min_versions (vector, timescaledb), und der
          # Boot-Check der Integrationstests prüft sie gegen GENAU dieses gecachte
          # Image. Ein statischer Key fror das Image auf vector 0.8.2 ein, während
          # Mig 118 das Manifest auf 0.8.5/2.28.3 hob — CI rot auf v4.19.0, obwohl
          # Upstream :pg18 die Versionen längst trug (Befund 2026-07-25). Jede
          # Manifest-Regeneration zieht jetzt einen frischen Pull nach sich.
          key: docker-testcontainers-timescale-pg18-${{ runner.os }}-${{ hashFiles('go/internal/schemacontract/manifest.json') }}

      - name: Load or pull testcontainers Docker image
        run: |
          if [ -f ~/.cache/docker-images/timescaledb-ha-pg18.tar ]; then
            docker load -i ~/.cache/docker-images/timescaledb-ha-pg18.tar
          else
            docker pull timescale/timescaledb-ha:pg18
            mkdir -p ~/.cache/docker-images
            docker save timescale/timescaledb-ha:pg18 -o ~/.cache/docker-images/timescaledb-ha-pg18.tar
          fi

      - name: Start docker-events recorder
        run: |
          # Runs for the job's lifetime; the post-mortem step reads the file.
          # `docker events` names WHO ends a container (die/kill/oom + exit
          # code + signal) — docker inspect's OOMKilled flag is notoriously
          # false-negative under cgroup v2, and the ring buffer may be empty.
          nohup docker events \
            --filter type=container \
            --format '{{.Time}} {{.Action}} {{.Actor.Attributes.name}} image={{.Actor.Attributes.image}} exitCode={{.Actor.Attributes.exitCode}} signal={{.Actor.Attributes.signal}}' \
            > "$RUNNER_TEMP/docker-events.log" 2>&1 &

      - name: Resolve shard packages
        id: shard
        working-directory: go
        run: |
          # The `rest` shard is the complement of every explicitly-sharded
          # path — the union of all shards is ./... BY CONSTRUCTION, so a new
          # package can never fall between the shards. The list is echoed so
          # each run documents its actual coverage.
          # The alternation below is that construction's ONLY moving part and
          # must name every explicitly-sharded path: a missing entry runs its
          # package TWICE (once in its own shard, once here), a stale entry
          # drops it from every shard and it goes silently untested.
          if [ -n "${{ matrix.packages }}" ]; then
            PKGS="${{ matrix.packages }}"
          else
            PKGS=$(go list ./... | grep -vE '/internal/(handler|store|overview|guard|events|rrf|dream|distillsource)(/|$)' | tr '\n' ' ')
          fi
          echo "Shard '${{ matrix.shard }}' packages: ${PKGS}"
          echo "packages=${PKGS}" >> "$GITHUB_OUTPUT"

      - name: Run integration tests
        working-directory: go
        run: |
          # -timeout is PER PACKAGE, and since 2026-08-29 per shard
          # (matrix.pkg_timeout): 15m everywhere, 30m for distill — see that
          # matrix entry for the measurement the 30m comes from.
          # Historically internal/handler exceeded 10m on nightly runners twice
          # (2026-07-26 + 2026-08-02) when the suite still booted ~500
          # containers; since the template-database harness (internal/testdb,
          # 2026-08-20) each package boots ONE container and copies a migrated
          # template per test, so packages run far below the budget. 15m stays
          # as weather headroom; a REAL hang still fails.
          # Coverage + -v only on nightly/dispatch — same rationale as the
          # unit job: unconsumed on push runs, atomic instrumentation costs
          # real minutes on the pipeline's critical path.
          EXTRA_FLAGS=""
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            EXTRA_FLAGS="-v -coverprofile=coverage-integration.out -covermode=atomic"
          fi
          # -p 1: one package binary — and therefore ONE shared Postgres
          # container — at a time. The default (= NumCPU = 2 on these
          # runners) ran two clusters with concurrent bulk seeds; three
          # heavy-shard container deaths (runs 32363351979/32365157495/
          # 32366043025, each mid-seed, exit=137, oom_killed=false, empty
          # ring buffer) all happened under that doubled load profile the
          # old seconds-lived containers never produced.
          go test \
            $EXTRA_FLAGS \
            -tags=integration \
            -count=1 \
            -p 1 \
            -timeout=${{ matrix.pkg_timeout }} \
            ${{ steps.shard.outputs.packages }}

      - name: Container post-mortem (on failure)
        if: failure()
        run: |
          # The template-database harness shares ONE Postgres container per
          # test binary — if it dies mid-suite (first suspect on 7GB runners:
          # OOM), every later test fails with connection resets and the root
          # cause is invisible in Go output. This dumps the hard evidence:
          # docker's OOMKilled flag + exit code + the container's last log
          # lines, for every container of the test image.
          for id in $(docker ps -aq --filter "ancestor=timescale/timescaledb-ha:pg18"); do
            echo "=== container ${id} ==="
            docker inspect "${id}" --format 'state={{.State.Status}} exit={{.State.ExitCode}} oom_killed={{.State.OOMKilled}} started={{.State.StartedAt}} finished={{.State.FinishedAt}}'
            docker logs --tail 60 "${id}" 2>&1 || true
          done
          echo "=== docker events (recorder) ==="
          grep -vE " (create|attach|connect|disconnect|start|exec_) " "$RUNNER_TEMP/docker-events.log" 2>/dev/null | tail -40 || true
          echo "=== kernel log (oom traces) ==="
          # dmesg came back EMPTY on these runners with stderr swallowed —
          # try both sources unfiltered-tail so an access failure is visible.
          sudo dmesg | tail -30 || echo "dmesg failed"
          sudo journalctl -k --no-pager | grep -iE "oom|killed process" | tail -20 || true
          echo "=== memory snapshot ==="
          free -m || true

      - name: Upload integration coverage
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: coverage-integration-${{ matrix.shard }}
          path: go/coverage-integration.out
          retention-days: 7

  # ─── Stage 4: Build Binary ────────────────────────────────────────────────────
  # This job is a flag-parity pre-probe, not a release build: it is the only
  # place that compiles ./cmd/ctxd for linux/amd64 with CGO_ENABLED=0 AND
  # -trimpath -s -w, one stage before the docker job builds the same target
  # inside the image (go/Dockerfile:18, `context: ./go`) — a flag or toolchain
  # breakage goes red here instead of inside the image build. The binary stays
  # in this job; the image always builds its own (no COPY ctxd). The effective
  # release flags are elsewhere: release.yml, -X main.* against ./cmd/ctx.
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint, unit-tests]
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go/go.sum

      - name: Build binary
        working-directory: go
        env:
          CGO_ENABLED: "0"
          GOOS: linux
          GOARCH: amd64
        run: |
          go build \
            -trimpath \
            -ldflags="-s -w" \
            -o ../ctxd \
            ./cmd/ctxd/

      - name: Verify binary
        run: |
          file ctxd

  # ─── Stage 5: Docker Build + Push ─────────────────────────────────────────────
  docker:
    name: Docker Build + Push
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # web gates the image push like lint gates the binary build: a red
    # frontend job must not publish an image (the Dockerfile embeds web/dist,
    # so this only moves the red earlier and names it — wave PV8).
    needs: [build, integration-tests, web]
    # Off-schedule gate (PV10): the nightly run is a verification pass for the
    # live tier — it must never re-push an image of an unchanged ref.
    if: >-
      github.event_name != 'schedule' &&
      (github.ref == 'refs/heads/root' ||
      github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/root' }}
            type=sha,prefix=sha-,format=short
            type=raw,value={{date 'YYYY-MM-DD'}}
          labels: |
            org.opencontainers.image.title=ctx
            org.opencontainers.image.description=The memory your LLM pretends to have. GottZ 4-Way RRF + GottZ Scope Model.
            org.opencontainers.image.url=https://github.com/GottZ/ctx
            org.opencontainers.image.source=https://github.com/GottZ/ctx
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=MPL-2.0

      - name: Build and push Docker image
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        with:
          context: ./go
          file: ./go/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: |
            type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
            type=gha
          cache-to: |
            type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max
            type=gha,mode=max
